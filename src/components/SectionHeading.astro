---
interface Props {
  id: string;
}
const { id } = Astro.props;
---

<h2
  id={id}
  class="flex items-center gap-4 text-sm font-bold tracking-[0.2em] text-muted uppercase"
>
  <span>
    <slot />
  </span>
  <span aria-hidden="true" class="h-px flex-1 bg-border"></span>
</h2>
