---
interface Props {
  id: string;
}
const { id } = Astro.props;
---

<h2
  id={id}
  class="neon font-script text-4xl leading-none lowercase sm:text-5xl"
>
  <slot />
</h2>

<style>
  .neon {
    color: var(--neon-cyan);
    text-shadow: var(--neon-cyan-glow);
  }
</style>
